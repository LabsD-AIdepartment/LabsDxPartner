# Native staff navigation and responsive acceptance

2026-09-11, D114. `/ops/access` now uses the existing StaffShell used by `/ops/ads` and `/ops/periods`. It previously rendered the partner AppShell and its partner shortcut rail despite being an authenticated staff workspace. The shared `nativeStaffRoutes(marketingEnabled)` map supplies all three native consoles with the real access/ads/periods paths. The marketing flag hides the ad entry when disabled. Account and re-verification links, selection/forms/queries and server capability checks are retained. Generic preview shell routing and the partner presentation are unchanged.

Native HTTPS build `sy69kiPeL4oS51-r5Tg9t` was checked in the existing browser tab against the existing isolated synthetic database. Actual navigation from access to ads to periods and back worked using the visible staff links. The marketing staff test account already had access; no grants were changed. TikTok status was expanded on the ads page. No invitation, source verification, import, payout or membership command was submitted.

The measured matrix contains 18 unique cases: three native staff routes, actual CSS widths 280/375/1440, and Day/Dark. Each had document scroll width equal to client width. Rendered buttons, links, inputs, selects and textareas stayed within horizontal page bounds with computed font sizes at least 16px; none had internal horizontal overflow. Raw bounds for every measured control are retained in `.agent-work/20260911-ops-responsive/evidence/layouts.json`, with `layout-verification.json`. Requested override dimensions were compensated for the browser's existing scale and actual dimensions were checked after settling. The first uncorrected request375 measured416 and is separately preserved, not relabeled as375.

These are DOM geometry checks on the browser's CSS viewport. They do not prove native200% zoom, mobile hardware/LCP, a screenshot-perfect visual design, all expanded forms/dialogs, keyboard interaction with every control, or production readiness. Viewport override was reset; final native width1016 had no page overflow, Day theme restored and no captured console errors. The tab ends at `/ops/access`. `evidence/navigation-final.json` records a repeated visible-link traversal with each resulting URL plus final geometry, Day-theme control and captured empty console-error list.

Validation:16 tests across staff access, staff finance and connection UI passed; final6 staff-access tests passed after correcting a test-only unsupported matcher option. Full typecheck and isolated production build/fixture exclusion passed. Independent reviewer verified six frozen code/wrapper/test hashes and found no issue. App/src337 files matched the served isolated build. Initial failed typecheck/build logs remain in the local evidence record; final results supersede them. This is a staff navigation correction and measured responsive evidence, not a replacement of the approved celebrity dashboard design.
